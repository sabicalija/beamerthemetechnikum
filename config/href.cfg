\hypersetup{
	colorlinks = true, % Sets link text to colored rather than drawing rectangular border boxes
	% hidelinks = true, % Hides links (useful for printing)
	% allcolors = fhtw-blue!80, % Sets all link colors to blue
	linkcolor = black, % Sets the color of internal links (sections, pages, etc.)
	citecolor = fhtw-blue!80, % Sets the color of citations
	urlcolor = fhtw-blue!80, % Sets the color of URLs
}
