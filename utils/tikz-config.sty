% TikZ Configuration for Technikum Theme
% Styles and configurations for TikZ diagrams

% =============================================================================
% TIKZ LIBRARIES
% =============================================================================

\usetikzlibrary{arrows.meta}
\usetikzlibrary{backgrounds}
\usetikzlibrary{calc}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{fadings}
\usetikzlibrary{fit}
\usetikzlibrary{patterns}
\usetikzlibrary{positioning}
\usetikzlibrary{shapes.geometric}

% =============================================================================
% TIKZ STYLES
% =============================================================================

% Node styles
\tikzset{
    % Add node styles here as needed
}

% Arrow styles
\tikzset{
    % Add arrow styles here as needed
}

% Diagram-specific styles
\tikzset{
    % Add custom diagram styles here as needed
    legend/.style={
            font=\scriptsize,
            align=left,
            node distance=-0.25em,
            text width=9em,
            show background rectangle,
            inner frame xsep=1em,
            inner frame ysep=1em,
            background rectangle/.style={draw=none, fill=none},
            every node/.style={below=of current bounding box.south}
        }
}

\endinput
